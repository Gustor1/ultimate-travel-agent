# Claude routing

For travel planning, begin at `.agents/skills/travel-orchestrator/SKILL.md`, then load only the selected specialist skills and their linked shared references. Do not scan `docs/`, `examples/`, `src/`, or `tests/` unless the task is repository development or a selected skill requires a deterministic CLI command. Use artifact-backed evidence and compact ID handoffs instead of replaying full research.
